# Dependencies (build artifact — installed via `npm install`, never committed)
node_modules/

# npm debug logs
npm-debug.log*
.npm/

# Jest coverage output
coverage/

# Generic logs
*.log

# Environment files (never commit secrets/local config)
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db
